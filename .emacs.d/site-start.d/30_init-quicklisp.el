;(load "/home/ogin/.emacs.d/plugins/quicklisp.lisp")
;(quicklisp-quickstart:install :path ".emacs.d/plugins/quicklisp/")
;(ql:add-to-init-file)

;(ql:quickload :cl-ppcre)

;;; The following lines added by ql:add-to-init-file:
;; #-quicklisp
;; (let ((quicklisp-init (merge-pathnames ".emacs.d/plugins/quicklisp/setup.lisp"
;;                                        (user-homedir-pathname))))
;;   (when (probe-file quicklisp-init)
;;     (load quicklisp-init)))
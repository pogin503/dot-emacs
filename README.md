# セットアップ

自分用のEmacs設定

```
$ git clone https://github.com/syl20bnr/spacemacs $HOME/.emacs.d
$ mkdir ~/workspace
$ git clone https://github.com/pogin503/dot-emacs.git ~/workspace/dot-emacs
$ cd ~/workspace/dot-emacs
$ ./setup.sh
```
